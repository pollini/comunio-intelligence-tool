# --- Required: Comunio login ---
COMUNIO_USER=
COMUNIO_PASSWORD=

# --- League & balance ---
# Start budget in euros (e.g. 40_000_000 for "ohne Team + 40 Mio.")
START_BUDGET=40000000
# Season start (YYYY-MM-DD). Transfers/salaries before this date are ignored.
SEASON_START=
# true if your league uses player salaries (Pro Player)
SALARIES_ENABLED=false
# Points payout per point: with salaries / without (Comunio default 30000 / 10000)
# POINTS_PAYOUT_WITH_SALARIES=30000
# POINTS_PAYOUT_WITHOUT_SALARIES=10000

# --- Seed for squad reconstruction (optional) ---
# Path to seed file, e.g. data/seed_squads_2025-05-27.json
# When set, salaries are computed from reconstructed squads + quote history.
# SEED_SQUADS_PATH=data/seed_squads_2025-05-27.json

# --- API ---
# COMUNIO_BASE_URL=https://www.comunio.de

# --- Cache (seconds) ---
# League overview cache TTL (default 60)
# CACHE_TTL_SECONDS=60
# News/transfer cache TTL (default 900). For fresher data 0:00–4:00 use e.g. 300.
# NEWS_CACHE_TTL_SECONDS=900

# --- Transfer cut-off ---
# Hour (MEZ/CEST) before which transfers count as previous day (default 4)
# CUTOFF_HOUR_MEZ=4

# --- Frontend ---
# Default UI language: de | en (default de)
# DEFAULT_LANGUAGE=de

# --- Debug ---
# DEBUG_PLAYER_COUNT=0
# DEBUG_SQUAD_COMPARE=0
